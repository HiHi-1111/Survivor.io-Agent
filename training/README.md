# Local training outputs

This directory stores local, resumable optimizer training artifacts. Model checkpoints,
gate statistics, and JSONL run logs are ignored by Git. Keep `.gitkeep` and this README in
the repository; keep private account profiles and generated checkpoints local.
